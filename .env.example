# Supabase — fill these in for the auth phase (Phase 3)
# Project Settings -> API Keys. Use the publishable key (sb_publishable_...) or legacy anon key.
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-public-key
